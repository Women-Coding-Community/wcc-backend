spring:
  config:
    activate:
      on-profile: test

  mail:
    host: localhost
    port: 3025
    username: test@wcc.dev
    password: test

security:
  token:
    ttl-minutes: 60
  api:
    key: test-api-key

file:
  storage:
    directory: data-test

app:
  seed:
    admin:
      enabled: true
      email: admin@wcc.dev
      password: wcc-admin