spring:
  config:
    activate:
      on-profile: test

security:
  api:
    key: test-api-key

file:
  storage:
    directory: data-test

surrealdb:
  host: localhost
  namespace: test_namespace
  database: test_db
  timeout-seconds: 80