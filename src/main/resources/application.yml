spring:
  application:
    name: wcc-backend
  profiles:
    active: default

  datasource:
    url: jdbc:postgresql://localhost:5432/wcc
    username: postgres
    password: MFpFnhhICniFNPA
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
    clean-disabled: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:test@email.com}
    password: ${MAIL_PASSWORD:your-app-password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

backend:
  app:
    url: ${SPRING_APP_BASE_URL:http://localhost:8080}

mentorship:
  validation:
    enabled: false

notification:
  mentor-profile-url: https://www.womencodingcommunity.com/mentors?keywords=
  volunteer-url: https://www.womencodingcommunity.com/volunteer

logging:
  level:
    org:
      springframework:
        jdbc: DEBUG
      flywaydb: DEBUG

security:
  enabled: true
  token:
    ttl-minutes: 60
  api:
    key: local

file:
  storage:
    directory: data

app:
  cors:
    allowed-origins: http://localhost:3000,https://localhost:3000
  email:
    team-signature: "WCC Mentorship Team"
  reset-password:
    base-url: http://localhost:3000
    ttl-minutes: 60
    reset-path: /reset-password
  seed:
    admin:
      enabled: true
      email: admin@wcc.dev
      password: wcc-admin

storage:
  type: local
  folders:
    main-folder: folder-id
    resources-folder: folder-id
    events-folder: folder-id
    mentors-folder: folder-id
    mentors-profile-folder: folder-id
    images-folder: folder-id

management:
  endpoint:
    health:
      show-details: always

  endpoints:
    web:
      exposure:
        include: "health,info,metrics,env,loggers"

springdoc:
  show-actuator: true
  api-docs:
    enabled: true
    path: /api-docs
  swagger-ui:
    enabled: true
    tagsSorter: alpha
    operations-sorter: alpha
  paths-to-match: /api/**, /actuator/**
