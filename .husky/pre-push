base_ref=$(git merge-base HEAD @{u} 2>/dev/null || git rev-parse HEAD^ 2>/dev/null || echo HEAD)
if git diff --name-only "$base_ref" HEAD | grep -q "^admin-wcc-app/"; then
  cd admin-wcc-app && npm run build
fi
